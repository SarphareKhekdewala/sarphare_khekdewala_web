# PRODUCTION ENVIRONMENT VARIABLES FOR VERCEL
# Copy these to your Vercel project settings: Settings > Environment Variables

# Database (REQUIRED)
# Use MongoDB Atlas free tier: https://www.mongodb.com/cloud/atlas
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/sarphare-khekdewala?retryWrites=true&w=majority"

# NextAuth (REQUIRED)
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-long-random-secret-key-here"
NEXTAUTH_URL="https://your-domain.vercel.app"

# Razorpay (REQUIRED for payments)
# Get from: https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID="your_razorpay_key_id"
RAZORPAY_KEY_SECRET="your_razorpay_key_secret"

# Email Configuration (REQUIRED for order confirmations)
# For Gmail: Enable 2FA and create an App Password
# Settings > Security > App Passwords
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_USER="your-business-email@gmail.com"
EMAIL_PASSWORD="your-16-digit-app-password"
EMAIL_FROM="Sarphare Khekdewala <noreply@your-domain.com>"

# Admin Credentials (REQUIRED)
# Change these to secure values
ADMIN_EMAIL="admin@your-domain.com"
ADMIN_PASSWORD="create-a-strong-password-here"

# IMPORTANT NOTES:
# 1. Never commit this file with real values to git
# 2. Add all these variables in Vercel Dashboard
# 3. After deployment, run: npx prisma db push (in Vercel console or locally with production DATABASE_URL)
# 4. Then run: npm run db:seed (to add sample products)
# 5. Change admin password immediately after first login
